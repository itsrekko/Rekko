import Typography from '@material-ui/core/Typography';

export const Title = () => {
    return(
      <Typography style={{
        color: '#000000',
        fontWeight: 400,
        lineHeight: '40.95px',
        fontSize: '35px'
    }}>
      Rekko
      </Typography>
    );
}