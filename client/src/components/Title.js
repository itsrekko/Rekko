import {Typography} from '@mui/material';

export const Title = () => {
    return(
      <Typography style={{
        color: '#000000',
        fontWeight: 400,
        lineHeight: '40.95px',
        fontSize: '35px'
    }}>
      Rekko
      </Typography>
    );
}